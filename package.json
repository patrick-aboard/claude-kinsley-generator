{
  "name": "kinsley-generator",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "bunx concurrently -k -n \"server,client\" -c \"blue,green\" \"bun --cwd server run dev\" \"bun --cwd client run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
